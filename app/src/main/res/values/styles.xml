<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="cardTheme">
        <item name="android:textColorHint">@color/red600</item>//hint color And Label Color in False State//hint color And Label Color in False State
        <item name="colorAccent">@color/colorAccent</item> //Label color in True State And Bar Color False And True State
        <item name="colorControlNormal">@color/red600</item>
        <item name="colorControlActivated">@color/btnsignuptext</item>
    </style>

</resources>
